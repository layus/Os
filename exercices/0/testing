#!/usr/bin/tcc -run

#include <stdio.h>
void
main()
{
	int i = 0;
	int nb = 1;
	for( ; i<nb; i++){
		printf("%d; %d\n", 'a' + ('z'-'a'+1)*(i)/nb,
                        	   'a' + ('z'-'a'+1)*(i+1)/nb-1);
        }
}
